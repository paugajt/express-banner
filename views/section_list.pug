extends layout

block content
	h1= title

	ul
	each section in section_list
		li
			a(href=section.url) #{section.course.course_name}
			| Section #: #{section.section_number} Professor: #{section.professor.name}

	else
		li There are no sections.